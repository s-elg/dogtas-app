import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: '#444444',
        margin: 10,
        padding: 10,
        borderRadius: 12,
    },

    input: {
       color: 'white' 
    },
})